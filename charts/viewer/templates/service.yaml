apiVersion: v1
kind: Service
metadata:
  name: {{ include "viewer.fullname" . }}
  labels:
    {{- include "viewer.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    name: http
  selector:
    {{- include "viewer.selectorLabels" . | nindent 4 }}
