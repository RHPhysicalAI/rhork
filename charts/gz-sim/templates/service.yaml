apiVersion: v1
kind: Service
metadata:
  name: {{ include "gzsim.fullname" . }}-ws
  labels:
    {{- include "gzsim.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: 9002
    targetPort: ws
    name: ws
  selector:
    {{- include "gzsim.selectorLabels" . | nindent 4 }}
